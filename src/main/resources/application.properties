spring.application.name=btgpactual
# EB proxy espera 5000; local usar -Dserver.port=8080 si prefieres 8080
server.port=${PORT:5000}

# MongoDB: definir MONGODB_URI en variable de entorno (no commitear credenciales)
spring.data.mongodb.uri=${MONGODB_URI}
